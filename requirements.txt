# Tabclose Cloud Run Job runtime deps.
# Pinned to the exact versions the offline test suite passes against
# (.venv), so the container cannot drift from what we verified locally.
google-adk==2.8.0
google-genai==2.20.0
google-cloud-firestore==2.29.0
google-cloud-storage==3.13.1
opentelemetry-api==1.42.1
opentelemetry-sdk==1.42.1
opentelemetry-exporter-gcp-trace==1.9.0
httpx==0.28.1
requests==2.34.2
